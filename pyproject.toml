[project]
name = "taskmanager"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
  "Flask==3.0.3",
  "Flask-SQLAlchemy==3.1.1",
  "Flask-Login==0.6.3",
  "python-dotenv==1.0.1",
  "gunicorn==22.0.0",
  "pg8000==1.31.5"
]

[project.scripts]
app = "backend.app:app"
